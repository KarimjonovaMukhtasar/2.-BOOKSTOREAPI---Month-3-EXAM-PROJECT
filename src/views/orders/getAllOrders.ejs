<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders - BookstoreAPI</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4CAF50;
            /* Green */
            --primary-dark: #388E3C;
            --secondary-color: #2196F3;
            /* Blue */
            --accent-color: #FFC107;
            /* Amber */
            --text-color: #333;
            --light-text-color: #666;
            --bg-color-light: #f4f7f6;
            --bg-color-dark: #e0e4e8;
            --card-bg: #ffffff;
            --border-color: #e0e0e0;
            --shadow-light: rgba(0, 0, 0, 0.05);
            --shadow-medium: rgba(0, 0, 0, 0.1);
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-color-light);
            color: var(--text-color);
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 0 20px;
        }

        .alert-success {
            background-color: #d4edda;
            color: #155724;
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 25px;
            border: 1px solid #c3e6cb;
            font-size: 0.95em;
            display: flex;
            align-items: center;
        }

        .alert-success svg {
            margin-right: 10px;
            color: #28a745;
        }

        h1 {
            text-align: center;
            color: var(--text-color);
            margin-bottom: 30px;
            font-weight: 600;
            font-size: 2.2em;
        }

        .header-actions {
            display: flex;
            justify-content: flex-end;
            /* Align to right */
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            padding: 12px 25px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            font-size: 0.95em;
            text-align: center;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
            border: 1px solid var(--primary-color);
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            border-color: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px var(--shadow-medium);
        }

        .orders-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .order-card {
            background-color: var(--card-bg);
            border-radius: 10px;
            box-shadow: 0 4px 15px var(--shadow-light);
            padding: 25px;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .order-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px var(--shadow-medium);
        }

        .order-card-header {
            margin-bottom: 20px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 15px;
        }

        .order-id {
            font-size: 1.1em;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 8px;
        }

        .order-details p {
            margin: 8px 0;
            font-size: 0.95em;
            color: var(--light-text-color);
        }

        .order-details strong {
            color: var(--text-color);
        }

        .order-total {
            font-size: 1.3em;
            font-weight: 600;
            color: var(--primary-dark);
            margin-top: 15px;
        }

        .order-status {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 10px;
        }

        .status-pending {
            background-color: #fff3e0;
            color: #fb8c00;
        }

        /* Amber light */
        .status-completed {
            background-color: #e8f5e9;
            color: #43A047;
        }

        /* Green light */
        .status-cancelled {
            background-color: #ffebee;
            color: #e53935;
        }

        /* Red light */
        .status-shipped {
            background-color: #e3f2fd;
            color: #1e88e5;
        }

        /* Blue light */

        .order-actions {
            margin-top: 20px;
            text-align: right;
            border-top: 1px solid var(--border-color);
            padding-top: 15px;
        }

        .order-actions .btn-secondary {
            background-color: transparent;
            color: var(--secondary-color);
            border: 1px solid var(--secondary-color);
            padding: 8px 18px;
            font-size: 0.9em;
        }

        .order-actions .btn-secondary:hover {
            background-color: var(--secondary-color);
            color: white;
            transform: translateY(-1px);
            box-shadow: 0 2px 8px rgba(33, 150, 243, 0.2);
        }

        @media (max-width: 768px) {
            .orders-grid {
                grid-template-columns: 1fr;
            }

            .header-actions {
                justify-content: center;
            }

            h1 {
                font-size: 1.8em;
            }

            .container {
                padding: 0 15px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>My Orders</h1>

        <div class="header-actions">
            <a href="/orders/new" class="btn btn-primary">
                + Create New Order
            </a>
        </div>

        <% if (orders && orders.length> 0) { %>
            <div class="orders-grid">
                <% orders.forEach(order=> { %>
                    <% if(order.user_id===user.id) { %>
                        <div class="order-card">
                            <div class="order-card-header">
                                <div class="order-id">Order #<%= order.id.substring(0, 8) %>
                                </div>
                                <span class="order-status status-<%= order.status.toLowerCase().replace(/\s/g, '-') %>">
                                    <%= order.status %>
                                </span>
                            </div>
                            <div class="order-actions">
                                <a href="/orders/<%= order.id %>" class="btn btn-secondary">View Details</a>
                            </div>
                        </div>
                        <% } %>
                            <% }) %>
            </div>
            <% } else { %>
                <p style="text-align: center; margin-top: 50px; font-size: 1.1em; color: var(--light-text-color);">
                    You haven't placed any orders yet. Why not create one?
                </p>
                <% } %>
    </div>
</body>

</html>