<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users - BookstoreAPI</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #3f51b5;
            --primary-dark: #303f9f;
            --accent-green: #4CAF50;
            --accent-green-dark: #388E3C;
            --accent-red: #f44336;
            --accent-red-dark: #d32f2f;
            --text-dark: #333;
            --text-light: #666;
            --bg-light: #f8f9fa;
            --card-bg: #ffffff;
            --border-color: #e0e0e0;
            --shadow-light: rgba(0, 0, 0, 0.08);
            --shadow-medium: rgba(0, 0, 0, 0.15);
            --border-radius-main: 12px;
            --border-radius-sm: 8px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-light);
            color: var(--text-dark);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            max-width: 1000px;
            width: 100%;
            margin: 20px auto;
            padding: 30px;
            background-color: var(--card-bg);
            border-radius: var(--border-radius-main);
            box-shadow: 0 10px 30px var(--shadow-medium);
            border: 1px solid var(--border-color);
        }

        h1 {
            text-align: center;
            color: var(--primary-color);
            margin-bottom: 30px;
            font-size: 2.5em;
            font-weight: 700;
        }

        .top-controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 30px;
        }

        .search-form {
            display: flex;
            flex-grow: 1;
            gap: 10px;
            max-width: 450px;
        }

        .search-input {
            flex-grow: 1;
            padding: 12px 18px;
            border-radius: var(--border-radius-sm);
            border: 1px solid var(--border-color);
            font-size: 1em;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(63, 81, 181, 0.2);
        }

        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: var(--border-radius-sm);
            cursor: pointer;
            color: white;
            font-size: 1em;
            font-weight: 500;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .btn-success {
            background-color: var(--accent-green);
            box-shadow: 0 4px 10px rgba(76, 175, 80, 0.2);
        }

        .btn-success:hover {
            background-color: var(--accent-green-dark);
            transform: translateY(-2px);
        }

        .message-box {
            background-color: #e8f5e9;
            color: #2e7d32;
            padding: 15px;
            border-radius: var(--border-radius-sm);
            margin-bottom: 25px;
            border: 1px solid #c8e6c9;
            font-size: 0.95em;
            text-align: center;
        }

        .error-box {
            background-color: #ffebee;
            color: #c62828;
            padding: 15px;
            border-radius: var(--border-radius-sm);
            margin-bottom: 25px;
            border: 1px solid #ffcdd2;
            font-size: 0.95em;
            text-align: center;
        }

        .users-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
        }

        .user-card {
            background-color: var(--card-bg);
            border-radius: var(--border-radius-sm);
            box-shadow: 0 4px 15px var(--shadow-light);
            padding: 25px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            position: relative;
            cursor: pointer;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }

        .user-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px var(--shadow-medium);
        }

        .user-card h3 {
            margin: 0 0 10px 0;
            color: var(--primary-color);
            font-size: 1.4em;
            font-weight: 600;
        }

        .user-card p {
            margin: 5px 0;
            color: var(--text-light);
            font-size: 0.95em;
        }

        .user-card-actions {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }

        .action-button {
            flex: 1;
            padding: 10px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            color: white;
            font-size: 0.9em;
            font-weight: 500;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-edit { background-color: var(--accent-green); }
        .btn-edit:hover { background-color: var(--accent-green-dark); transform: translateY(-2px); }

        .btn-delete { background-color: var(--accent-red); }
        .btn-delete:hover { background-color: var(--accent-red-dark); transform: translateY(-2px); }

        .no-users-found {
            text-align: center;
            color: var(--text-light);
            margin-top: 50px;
            font-size: 1.1em;
            padding: 20px;
            border: 1px dashed var(--border-color);
            border-radius: var(--border-radius-sm);
        }

        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 8px;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .pagination-link {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 10px 15px;
            border: 1px solid var(--border-color);
            border-radius: var(--border-radius-sm);
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            min-width: 40px;
            background-color: var(--card-bg);
        }

        .pagination-link:hover {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(63, 81, 181, 0.2);
        }

        .pagination-link.active {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }

        .pagination-link.disabled {
            background-color: #f0f0f0;
            color: var(--text-light);
            cursor: not-allowed;
            opacity: 0.7;
            pointer-events: none;
        }

        @media (max-width: 768px) {
            .top-controls { flex-direction: column; align-items: stretch; }
            .search-form { max-width: 100%; width: 100%; }
            .btn-new-user { width: 100%; text-align: center; }
            .users-grid { grid-template-columns: 1fr; }
            h1 { font-size: 2em; }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>All Users</h1>

        <div class="top-controls">
            <form action="/users" method="GET" class="search-form">
                <input type="text" name="query" placeholder="Search by username, email..." class="search-input" value="<%= searchQuery || '' %>">
                <button type="submit" class="btn btn-success">Search</button>
            </form>
            <a href="/users/new" class="btn btn-success btn-new-user">+ Add New User</a>
        </div>

        <% if (message) { %>
            <div class="message-box"><%= message %></div>
        <% } %>

        <% if (errors && errors.length > 0) { %>
            <div class="error-box">
                <% errors.forEach(error => { %>
                    <p><%= error.msg %></p>
                <% }) %>
            </div>
        <% } %>

        <% if (users && users.length > 0) { %>
            <div class="users-grid">
                <% users.forEach(user => { %>
                    <div class="user-card">
                        <a href="/users/<%= user.id %>" class="full-card-link"></a>
                        <h3><%= user.username %></h3>
                        <p><strong>Email:</strong> <%= user.email %></p>
                        <p><strong>Name:</strong> <%= user.first_name %> <%= user.last_name %></p>
                        <p><strong>Phone:</strong> <%= user.phone_number %></p>
                        <p><strong>Address:</strong> <%= user.address %></p>
                        <p><strong>Role:</strong> <%= user.role %></p>
                        <p><strong>Status:</strong> <%= user.status %></p>
                        <div class="user-card-actions">
                            <a href="/users/<%= user.id %>/edit" class="action-button btn-edit">Edit</a>
                            <button class="action-button btn-delete" onclick="deleteUser('<%= user.id %>')">Delete</button>
                        </div>
                    </div>
                <% }) %>
            </div>

            <div class="pagination">
                <% if (pagination) { %>
                    <% if (pagination.prevPage) { %>
                        <a href="?page=<%= pagination.prevPage %>" class="pagination-link">&laquo; Prev</a>
                    <% } %>
                    <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                        <a href="?page=<%= i %>" class="pagination-link <%= i === pagination.currentPage ? 'active' : '' %>"><%= i %></a>
                    <% } %>
                    <% if (pagination.nextPage) { %>
                        <a href="?page=<%= pagination.nextPage %>" class="pagination-link">Next &raquo;</a>
                    <% } %>
                <% } %>
            </div>
        <% } else { %>
            <p class="no-users-found">No users found<%= searchQuery ? ` for "${searchQuery}"` : '' %>. Try adjusting your search or add a new user!</p>
        <% } %>
    </div>

    <script>
        function deleteUser(id) {
            if(confirm('Are you sure you want to delete this user?')) {
                fetch(`/users/${id}/delete`, {method: 'POST'})
                    .then(() => location.reload());
            }
        }
    </script>
</body>
</html>
